#include <iostream>

using namespace std;

int main() {
    long long int input, out;
    cin >> input;
    out = (input) * (input - 1) * (input - 2) / 6;
    cout << out << endl << 3;

    return 0;
}
